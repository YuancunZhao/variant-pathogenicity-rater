[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "variant-pathogenicity-rater-mcp"
version = "0.3.0"
description = "Extensible MCP server framework for ACMG variant interpretation tools."
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2,<3",
]

[project.scripts]
vpr = "variant_pathogenicity_rater.cli:main"

[project.optional-dependencies]
dev = [
    "pytest>=8,<9",
]
test = [
    "pytest>=8,<9",
]
coverage = [
    "pytest-cov>=5,<7",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
include = ["variant_pathogenicity_rater*"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
addopts = [
    "--strict-config",
    "--strict-markers",
]
markers = [
    "online_clinvar_smoke: opt-in live ClinVar smoke tests; skipped unless explicit ClinVar online env gates are set",
    "online_gnomad_smoke: opt-in live gnomAD smoke tests; skipped unless explicit gnomAD online env gates are set",
    "online_vep_smoke: opt-in live Ensembl VEP smoke tests; skipped unless explicit VEP online env gates are set",
    "online_literature_smoke: opt-in live PubMed/LitVar smoke tests; skipped unless explicit literature online env gates are set",
]

[tool.coverage.run]
branch = true
source = ["variant_pathogenicity_rater", "mcp-server"]
omit = [
    "tests/*",
    "*/__pycache__/*",
]

[tool.coverage.report]
show_missing = true
skip_covered = false
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "raise NotImplementedError",
]
